# DecodelabsTechStackRecommender
Built a content-based recommendation engine that matches user skills to job roles using TF-IDF vectorization and Cosine Similarity, with a graceful fallback for the cold-start problem.
